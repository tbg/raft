initSidebarItems({"struct":[["Server","The `Server` is responsible for receiving events from remote `Server` or `Client` instances, as well as setting election and heartbeat timeouts.  When an event is received, it is applied to the local `Consensus`. The `Consensus` may optionally return a new event which must be dispatched to either the `Server` or `Client` which sent the original event, or to all `Server` instances."]]});